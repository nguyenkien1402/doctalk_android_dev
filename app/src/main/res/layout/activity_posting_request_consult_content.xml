<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context=".consult_activities.PostingRequestConsultActivity"
        tools:showIn="@layout/activity_posting_request_consult">
            <EditText
                android:padding="10dp"
                android:hint="Tell me your problem"
                android:id="@+id/posting_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textMultiLine"
                android:maxLines="100"
                android:background="#00000000"
                android:scrollbars="vertical"/>

            <HorizontalScrollView
                android:id="@+id/posting_hori_scroll_view"
                android:layout_width="match_parent"
                android:layout_height="250dp"
                android:layout_marginTop="8dp"
                android:background="#eee"
                android:clipToPadding="false"
                android:paddingStart="8dp"
                android:paddingTop="16dp"
                android:paddingEnd="0dp"
                android:paddingBottom="16dp"
                android:layout_marginBottom="16dp"
                android:scrollbarStyle="outsideOverlay">

                <LinearLayout
                    android:id="@+id/posting_selected_photos_container"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal" />
            </HorizontalScrollView>
    </LinearLayout>
</ScrollView>
